fn main() int {
    int k := 0 + (11 % 22);
    int o := second(2, 2);
    print(1,k,k+5, o);
    return 0;
}


fn second(int a, int b) int {
    int k := a + b;
    int d := a ^ b + 2;
    return k + d;
}