fn main() int {
    int a := 10 ^ 2;
    while(True) {
        int k := 17 + a;
        if(k < 100) {
            while(12 > a) {
                if(a + 17 > k + 23 * a) {
                    return 0;
                } else {
                    print(a,k,a,a);
                }
                a := a + 1;
            }
        } else {
            bool george := get_bool(a,k);
            if(george) {
                int j := add_one(add_one(add_one(0)));
            }
        }
    }
    
    int f := get_int(27) + get_another_int() + 178;
    return f;
}

fn get_bool(int first, int second) bool {
    return first > second;
}

fn add_one(int arg) int {
    return arg + 1;
}

fn get_int(int arg) int {
    if(True) {
        if(True) {
            if(True) {
                int j := 1600;
                if(True) {
                    while(True) {
                        j := j + 10;
                        if(j > 10000) {
                            break;
                        }
                    }
                } else {
                    return 270;
                }
            }
            if(arg < 100) {
                return 2;
            }
        }
    } else {
        return arg * 100000;
    }
}

fn get_another_int() int {
    return 887;
}